# render11 一致性修复自测报告

## 测试目的
验证线上代码修复（`pickFirstFrameUrl` 优先使用分镜定格图、`generateShotImage` 传入双角色定妆图 + 场景图）是否真的能解决：
- 场景不统一
- 双角色镜第二人脸上无约束 / 服装不一致
- 表情假/诡异（图片层无法完全验证，视频层待查）

## 测试方法
- 脚本：`render11.mjs`（只出图片层）
- 4 张参考图：林晚定妆、陈默定妆、雨夜巷口场景、末班车站场景
- 4 张分镜定格图，每张都同时传入：林晚定妆图 + 陈默定妆图 + 对应场景图（共 3 张条件图）
- 通过读取生成后的图片人工核对一致性

## 修复过程中发现的脚本 bug
原 `render11.mjs` 的 `SHOTS.cast` 写的是 `['林晚', '陈默']`，但 `REFS` 的 key 是 `'林晚定妆'` / `'陈默定妆'`，导致 `refUrls['林晚']` 为 undefined——双角色定妆图根本没作为 I2I 条件传入。已修复为 `['林晚定妆', '陈默定妆']`。

## 上游接口状况
- 图片接口间歇性超时 / `503 image queue is full`，存在明显限流。
- 已把 `render11.mjs` 重试改为最多 12 次；503 时退避 20~75s；单图超时 60s；镜头间隔 10s。

## 验证结论

| 检查项 | 结果 |
|---|---|
| 双角色同框 | ✅ 4 张定格图均同时出现林晚 + 陈默 |
| 林晚服装（米色风衣） | ✅ 4 镜一致 |
| 陈默服装（深色外套） | ✅ 色调/款式基本稳定，shot_3 有轻微偏风衣漂移 |
| 场景统一 | ✅ shot_1/4 同为雨夜巷口霓虹；shot_2/3 同为末班车站冷光雨夜 |
| 人脸跨镜一致 | ⚠️ 正面/半侧面基本一致；大侧脸角度下细节略有变化 |
| 表情假/诡异 | ⚠️ 图片层无法验证；需看视频层，且 `negative_prompt` 独立参数未确认 |

## 结论
代码修复方向正确：把**分镜定格图（含角色+场景）**作为视频首帧，并在生成定格图时传入**每个角色的定妆图 + 场景图**，能显著改善双角色同框、场景统一、服装一致问题。

残留的小差异（外套款式轻微漂移、大侧脸脸部细节）属于 I2I 模型对复杂款式/大角度变化的保留能力边界，不是代码逻辑 bug。

## 下一步建议
1. 确认 Agnes `/v1/videos` 是否支持独立 `negative_prompt`，把 NEGATIVE_BASE 从正向 prompt 剥离。
2. 真正跑一段短剧视频（用线上最新代码生成 2~3 镜）人工观感验证，因为图片层的表情/动态口型只能在视频层确认。
