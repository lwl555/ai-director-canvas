# 生产构建环境变量（仅在 CI / 构建时注入，不要提交真实值到仓库）
# 这些变量会被打进前端 bundle，因此只能放「公开安全」的值：
#   - Supabase 匿名 key（设计上就是公开的）
#   - Supabase Edge Function 的地址（函数名本身是公开路由）
# ⚠️ Agnes API key 绝对不要出现在这里、不要以 VITE_ 开头，
#    它只存在于 Supabase Edge Function 的 Deno secret（见 supabase/functions/agnes-proxy）。

# 指向你的 Supabase 代理函数（前端所有 Agnes 请求都走它，key 不进包）
VITE_AGNES_BASE=https://你的项目ref.functions.supabase.co/agnes-proxy

# Supabase（用于跨设备保存画布；不填则只用浏览器本地存储）
VITE_SUPABASE_URL=https://你的项目ref.supabase.co
VITE_SUPABASE_ANON=你的_supabase_anon_key
